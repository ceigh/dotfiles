#/usr/bin/env sh
git push --no-verify \
  && git checkout master \
  && git pull \
  && git push github \
  && git checkout development \
  && git merge master \
  && git push \
  && git push github \
