#/usr/bin/env sh
git push --no-verify \
  && git checkout master \
  && git pull \
  && git push --no-verify github \
  && git checkout development \
  && git merge master \
  && git push --no-verify \
  && git push --no-verify github \
